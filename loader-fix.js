// This file is no longer needed as the loader has been completely removed
// Keeping the file but disabling all functionality

document.addEventListener('DOMContentLoaded', () => {
    // No operation
});
